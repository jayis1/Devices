# WellSync ML Pipeline

Synthetic reference trainers for WellSync.

Each script:
- generates a deterministic CSV dataset into `artifacts/`;
- computes lightweight coefficients or summary metrics;
- writes a JSON artifact the backend can load or inspect.
