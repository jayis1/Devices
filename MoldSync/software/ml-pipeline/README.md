# MoldSync ML pipeline

Training scripts create baseline artifacts for room-level condensation, leak attribution, drying policy, and inspection scoring.
All scripts emit JSON metadata to `artifacts/`.
