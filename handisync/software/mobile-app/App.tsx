import React,{useState} from 'react'; import {SafeAreaView,Text,Button,Alert} from 'react-native';
export default function App(){const [armed,setArmed]=useState(false); const request=()=>Alert.alert('Send request?','The hub will require local safety checks and confirmation.',[{text:'Cancel'},{text:'Confirm',onPress:()=>{}}]); return <SafeAreaView style={{padding:24,gap:16}}><Text style={{fontSize:28}}>HandiSync</Text><Text>Local assistive controls. Hardware emergency stops remain authoritative.</Text><Button title={armed?'Controls armed':'Arm controls'} onPress={()=>setArmed(!armed)}/><Button title="Open mapped drawer" disabled={!armed} onPress={request}/></SafeAreaView>}
