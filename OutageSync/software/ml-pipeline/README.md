# OutageSync ML Pipeline

Baseline training scripts generate synthetic data if no dataset is present. The models are intended as architecture-ready starting points for:

- outage duration forecasting
- load shedding recommendation
- cold-chain safety estimation
- generator health anomaly scoring
- comfort decay forecasting

Artifacts are written to `artifacts/`.
